import { Meta, StoryObj } from '@storybook/react';

import { SmartRange } from './index';

export default {
  title: 'Components/SmartRange',
  component: SmartRange,
} as Meta;

export const Default: StoryObj = {};
